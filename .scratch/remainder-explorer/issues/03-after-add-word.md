# After Add word succeeds in Remainder explorer, what happens to pins and the bag?

Type: grilling
Map: [Remainder explorer spec](../map.md)

## Question

After Add word succeeds while Remainder explorer is on, what happens to the pin set and the Remainder bag?

Add word already returns a fresh Target progress payload (catalog word added, Target search resumed). Decide whether existing pins are kept, whether the new word is auto-pinned, whether the Add word input clears, and how the bag should look so a second invented word is still possible.
